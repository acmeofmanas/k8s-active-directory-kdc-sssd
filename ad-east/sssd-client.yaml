---
apiVersion: v1
kind: ConfigMap
metadata:
  name: sssd-config
  namespace: ad-east
data:
  sssd.conf: |
    [sssd]
    domains = east.local, west.local
    config_file_version = 2
    services = nss, pam
    debug_level = 3

    [domain/east.local]
    id_provider = ldap
    auth_provider = krb5
    chpass_provider = krb5
    debug_level = 3

    ldap_uri = ldap://samba-ad-dc.ad-east.svc.cluster.local
    ldap_search_base = dc=east,dc=local
    ldap_schema = ad
    ldap_id_mapping = true
    ldap_referrals = false

    ldap_sasl_mech = GSSAPI
    ldap_sasl_authid = sssd-svc@EAST.LOCAL
    ldap_krb5_keytab = /etc/sssd/east.keytab
    ldap_krb5_init_creds = true

    ldap_user_object_class = user
    ldap_user_name = sAMAccountName
    ldap_user_home_directory = unixHomeDirectory
    ldap_user_shell = loginShell
    ldap_group_object_class = group
    ldap_group_name = sAMAccountName
    ldap_group_member = member

    krb5_realm = EAST.LOCAL
    krb5_server = samba-ad-dc.ad-east.svc.cluster.local
    krb5_store_password_if_offline = true

    cache_credentials = true
    enumerate = true
    use_fully_qualified_names = false
    fallback_homedir = /home/%u
    default_shell = /bin/bash

    [domain/west.local]
    id_provider = ldap
    auth_provider = krb5
    chpass_provider = krb5
    debug_level = 3

    ldap_uri = ldap://samba-ad-dc.ad-west.svc.cluster.local
    ldap_search_base = dc=west,dc=local
    ldap_schema = ad
    ldap_id_mapping = true
    ldap_referrals = false

    ldap_sasl_mech = GSSAPI
    ldap_sasl_authid = sssd-svc@WEST.LOCAL
    ldap_krb5_keytab = /etc/sssd/west.keytab
    ldap_krb5_init_creds = true

    ldap_user_object_class = user
    ldap_user_name = sAMAccountName
    ldap_user_home_directory = unixHomeDirectory
    ldap_user_shell = loginShell
    ldap_group_object_class = group
    ldap_group_name = sAMAccountName
    ldap_group_member = member

    krb5_realm = WEST.LOCAL
    krb5_server = samba-ad-dc.ad-west.svc.cluster.local
    krb5_store_password_if_offline = true

    cache_credentials = true
    enumerate = true
    use_fully_qualified_names = false
    fallback_homedir = /home/%u
    default_shell = /bin/bash

  krb5.conf: |
    [libdefaults]
        default_realm = EAST.LOCAL
        dns_lookup_realm = false
        dns_lookup_kdc = false
        rdns = false
        forwardable = true

    [realms]
        EAST.LOCAL = {
            kdc = samba-ad-dc.ad-east.svc.cluster.local
            admin_server = samba-ad-dc.ad-east.svc.cluster.local
            default_domain = east.local
        }
        WEST.LOCAL = {
            kdc = samba-ad-dc.ad-west.svc.cluster.local
            admin_server = samba-ad-dc.ad-west.svc.cluster.local
            default_domain = west.local
        }

    [domain_realm]
        .east.local = EAST.LOCAL
        east.local = EAST.LOCAL
        .west.local = WEST.LOCAL
        west.local = WEST.LOCAL

  start.sh: |
    #!/bin/bash
    set -e

    echo "==> Blocking service starts during package install..."
    printf '#!/bin/sh\nexit 101\n' > /usr/sbin/policy-rc.d
    chmod +x /usr/sbin/policy-rc.d
    ln -sf /bin/true /usr/local/bin/systemctl

    echo "==> Installing SSSD + Kerberos packages..."
    apt-get update -qq
    DEBIAN_FRONTEND=noninteractive apt-get install -y -qq --no-install-recommends \
        sssd-common sssd-ldap sssd-krb5 sssd-tools \
        libsss-idmap0 libsss-nss-idmap0 libnss-sss libpam-sss \
        krb5-user ldap-utils \
        libsasl2-modules-gssapi-mit libsasl2-modules || {
        echo "Fixing partially configured packages..."
        DEBIAN_FRONTEND=noninteractive dpkg --configure -a 2>&1 || true
        DEBIAN_FRONTEND=noninteractive apt-get install -y -f -qq 2>&1 || true
    }

    rm -f /usr/sbin/policy-rc.d /usr/local/bin/systemctl

    echo "==> Configs mounted from ConfigMap:"
    cat /etc/sssd/sssd.conf
    cat /etc/krb5.conf

    echo "==> Keytab: EAST"
    klist -k /etc/sssd/east.keytab
    echo "==> Keytab: WEST"
    klist -k /etc/sssd/west.keytab

    echo "==> Testing Kerberos — EAST.LOCAL..."
    kinit -k -t /etc/sssd/east.keytab sssd-svc@EAST.LOCAL
    klist; kdestroy
    echo "    EAST Kerberos OK"

    echo "==> Testing Kerberos — WEST.LOCAL..."
    kinit -k -t /etc/sssd/west.keytab sssd-svc@WEST.LOCAL
    klist; kdestroy
    echo "    WEST Kerberos OK"

    echo "==> Testing GSSAPI ldapsearch — EAST.LOCAL..."
    kinit -k -t /etc/sssd/east.keytab sssd-svc@EAST.LOCAL
    ldapsearch \
        -H ldap://samba-ad-dc.ad-east.svc.cluster.local \
        -Y GSSAPI \
        -b "DC=east,DC=local" \
        "(objectClass=user)" sAMAccountName 2>&1 \
        | grep -E "sAMAccountName:|result:" || true
    kdestroy

    echo "==> Testing GSSAPI ldapsearch — WEST.LOCAL..."
    kinit -k -t /etc/sssd/west.keytab sssd-svc@WEST.LOCAL
    ldapsearch \
        -H ldap://samba-ad-dc.ad-west.svc.cluster.local \
        -Y GSSAPI \
        -b "DC=west,DC=local" \
        "(objectClass=user)" sAMAccountName 2>&1 \
        | grep -E "sAMAccountName:|result:" || true
    kdestroy

    echo "==> Starting SSSD (both domains)..."
    sssd -D --logger=stderr &

    echo ""
    echo "======================================================"
    echo " SSSD Client ready — EAST.LOCAL + WEST.LOCAL"
    echo " Domains: east.local, west.local"
    echo ""
    echo "   getent passwd              list users from both ADs"
    echo "   getent group               list groups from both ADs"
    echo "   id manas                   look up user (east wins)"
    echo "   id manas@west.local        look up west user explicitly"
    echo "   sssctl domain-status east.local"
    echo "   sssctl domain-status west.local"
    echo "======================================================"
    tail -f /var/log/sssd/sssd_east.local.log 2>/dev/null &
    tail -f /var/log/sssd/sssd_west.local.log 2>/dev/null &
    tail -f /dev/null
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sssd-client
  namespace: ad-east
  labels:
    app: sssd-client
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sssd-client
  template:
    metadata:
      labels:
        app: sssd-client
    spec:
      containers:
        - name: sssd-client
          image: ubuntu:22.04
          command: ["/bin/bash", "/startup/start.sh"]
          securityContext:
            privileged: true
          volumeMounts:
            - name: startup
              mountPath: /startup/start.sh
              subPath: start.sh
              readOnly: true
            - name: sssd-conf
              mountPath: /etc/sssd/sssd.conf
              subPath: sssd.conf
              readOnly: true
            - name: krb5-conf
              mountPath: /etc/krb5.conf
              subPath: krb5.conf
              readOnly: true
            - name: east-keytab
              mountPath: /etc/sssd/east.keytab
              subPath: sssd.keytab
              readOnly: true
            - name: west-keytab
              mountPath: /etc/sssd/west.keytab
              subPath: sssd.keytab
              readOnly: true
            - name: sssd-run
              mountPath: /var/lib/sss
            - name: sssd-logs
              mountPath: /var/log/sssd
      volumes:
        - name: startup
          configMap:
            name: sssd-config
            items:
              - key: start.sh
                path: start.sh
            defaultMode: 0755
        - name: sssd-conf
          configMap:
            name: sssd-config
            items:
              - key: sssd.conf
                path: sssd.conf
            defaultMode: 0600
        - name: krb5-conf
          configMap:
            name: sssd-config
            items:
              - key: krb5.conf
                path: krb5.conf
            defaultMode: 0644
        - name: east-keytab
          secret:
            secretName: sssd-keytab
            defaultMode: 0600
        - name: west-keytab
          secret:
            secretName: sssd-keytab-west
            defaultMode: 0600
        - name: sssd-run
          emptyDir: {}
        - name: sssd-logs
          emptyDir: {}
